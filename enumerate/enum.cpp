#include <iostream>
#include <typeinfo>
using namespace std;

enum Color {
    BLACK,
    RED, 
    YELLOW,
    BLUE,
};

enum 

int main() {
    using namespace std;

    Color paint = BLACK;
    Color house(BLUE);
    Color apple{RED};

    return 0;
}

